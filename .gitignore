# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
.venv/
.env

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Jupyter Notebook
.ipynb_checkpoints/

# Environment variables
.env
.env.local
.env.production

# Model files (büyük dosyalar depoya yüklenmez)
models/*.bin
models/*.pt
models/*.onnx
models/*.safetensors
*.h5

# Data files (ham veri yüklenmez, işlenmiş veri yüklenebilir)
data/raw/*.pdf
data/raw/*.jpg
data/raw/*.png
data/raw/*.tiff

# GÜVENLİK: üretilen yerel artefaktlar depoya sızmasın
# (vektör DB ikili dosyaları, CLI çıktı dizini, model önbellekleri)
data/chroma_db/
chroma_db/
output/
.cache/
huggingface/

# Logs
*.log
logs/

# Coverage reports
htmlcov/
.coverage
.coverage.*
coverage.xml

# pytest
.pytest_cache/

# mypy
.mypy_cache/

# Streamlit
.streamlit/secrets.toml

# Demo calisma aninda uretilen girdiler (tarih-bagimli/binari)
demo/demo_evraklar/

# Calisma-zamani uretilen denetim izi ve geri bildirim log'u
# (her kosuda degisir; kod CREATE TABLE IF NOT EXISTS ile otomatik yeniden olusturur)
data/processed/kayit_defteri.db
data/processed/geri_bildirim.jsonl

# Claude Code kisisel yerel ayarlari (otomatik commit+push hook'u dahil).
# Kisiseldir; takim deposuna gitmez (herkese otomatik push dayatilmaz).
.claude/settings.local.json
# Panoyu onizleme icin uretilen tek kullanimlik statik HTML'ler
data/processed/onizleme_*.html

# loopkit — oturum-ici gecici/yerel dosyalar (commit edilmez)
claude-decisions.json          # PreCompact hook karar kaydi
feature_oneri_taslagi.md       # /next oneri taslagi
.mcp.json                      # yerel/opsiyonel MCP ayari (.mcp.json.ornek paylasilir)
