# ============================================================
# Opsiyonel bağımlılıklar — gelişmiş yetenekler
# Sistem bunlar OLMADAN da kural tabanlı modda tam çalışır.
# ============================================================

# --- OCR: taranmış PDF / görüntü evraklar ---
# (Ayrıca sistemde Tesseract kurulumu gerekir: brew install tesseract tesseract-lang)
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.17.0
easyocr>=1.7.0

# --- Semantik arama (hibrit mevzuat RAG'in opsiyonel yoğun katmanı) ---
# sentence-transformers: turkish-e5-large gömme (EMBEDDING_SEMANTIK_AKTIF=1)
# ve bge-reranker-v2-m3 yeniden sıralama (EMBEDDING_RERANK_AKTIF=1) için.
# Bayraklar bilinçli olarak varsayılan kapalıdır (offline-first: ilk açılışta
# model indirme gerektirir). chromadb yalnızca yedek arama yoludur.
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- Yerel model çalıştırma (HuggingFace) ---
transformers>=4.40.0
torch>=2.2.0

# --- Veri işleme / analiz ---
pandas>=2.0.0
numpy>=1.24.0

# --- Sunum üretimi (presentations/*.pptx) ---
python-pptx>=1.0.0
