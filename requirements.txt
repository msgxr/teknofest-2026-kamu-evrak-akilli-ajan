# ============================================================
# Teknofest 2026 — Kamu Evrak Akıllı Ajan Sistemi
# Çekirdek bağımlılıklar: metin (TXT) ve metin-katmanlı PDF girişiyle sistem
# TAM çalışır (11 ajan, hibrit RAG, taslak, yönlendirme, KVKK — LLM'siz).
#
# Not: LLM çağrıları stdlib (urllib) ile yapılır; OpenAI SDK, LangChain, torch
# vb. ÇEKİRDEK için gerekli değildir. TARANMIŞ PDF / GÖRÜNTÜ (OCR) girişi ve
# semantik arama OPSİYONELDİR: pip install -r requirements-optional.txt
# (+ görüntü OCR için sistemde Tesseract kurulumu; bkz. docs/model_bilgileri.md).
# ============================================================

# --- Temel ---
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- Evrak okuma (PDF metin katmanı) ---
# GÜVENLİK: PyPDF2 terk edilmiştir ve son sürümü (3.0.1) yamasız bir DoS
# açığı taşır (PYSEC-2026-1835: özel hazırlanmış PDF'te extract_text sonsuz
# döngü). Bakımlı ardılı pypdf ile değiştirildi (API aynı; >=6.13.3 hem bu
# açığı hem GHSA-jm82-fx9c-mx94 bellek DoS'unu kapatır).
pypdf>=6.13.3

# --- Web Arayüzü ---
# Kurumsal pano (app.py) Streamlit + gömülü tema ile çalışır; grafik ve tablo
# katmanı için pandas/altair doğrudan import edilir (Streamlit bunları transitif
# getirse de app.py doğrudan kullandığından açıkça sabitlenir — tekrarlanabilirlik).
streamlit>=1.30.0
pandas>=2.0.0
altair>=5.0.0

# --- Konsol çıktısı ---
rich>=13.7.0

# --- Test ---
pytest>=7.0.0
pytest-cov>=4.0.0
